# This is a basic workflow to help you get started with Actions

name: Actions script

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Runs a single command using the runners shell
      - name: Echo message
        run: echo Sending message to Discord...

      - name: Webhook dialer
        uses: joelwmale/webhook-action@master
        with:
          url: 'https://discord.com/api/webhooks/792470234921566219/okuTBj5uiRnnQNtDmR1UmMBb2X542-1iE7tle3mYQNTaPH_zm93Xq68JPYZ3FRZn_06-'
          headers: |
            {
              "Content-Type": "application/json"
            }
          body: |
            {
              "username": "GitHub",
              "embeds": [{
                "title": "Новый коммит в основной ветке",
                "url": "https://github.com/paukahn/Syncro-frontend",
                "description": "Чтобы узнать подробности, нажмите на это сообщение"
              }]
            }
